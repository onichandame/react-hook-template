{
  "private": true,
  "name": "react-hook-template",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "nodemon": "^2.0.4",
    "prettier": "^2.0.5"
  },
  "prettier": {
    "printWidth": 80,
    "semi": false,
    "singleQuote": true,
    "trailingComma": "es5"
  },
  "husky": {
    "pre-commit": "echo commiting"
  },
  "scripts": {
    "dev": "yarn build:example && nodemon",
    "build": "yarn workspace react-hook build && yarn build:example",
    "build:example": "yarn workspace example build",
    "publish": "yarn workspace react-hook npm publish"
  }
}
